#include <iostream>
#include <iomanip>
#include <string>
#include <bits/stdc++.h>

using namespace std;

int calculate_num_of_decodings(int input)
{
    int default_value = 0;
    vector<vector<int>> tabulation_matrix;
    tabulation_matrix.resize()
}

int main()
{
#ifndef ONLINE_JUDGE
    freopen("xinput.txt", "r", stdin);
    freopen("xoutput.txt", "w", stdout);
#endif

    int input;
    cin >> input;
    while (input != 0)
    {
        int num_of_decodings = calculate_num_of_decodings(input);
        cin >> input;
    }
}
