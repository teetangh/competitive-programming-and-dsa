#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
#define fastio                        \
    {                                 \
        ios_base::sync_with_stdio(0); \
        cin.tie(0);                   \
        cout.tie(0);                  \
    }
using namespace std;

string decoding(string input, map<char, char> mp)
{
}

int main()
{
#ifndef ONLINE_JUDGE
    freopen("xinput.txt", "r", stdin);
    freopen("xoutput.txt", "w", stdout);
#endif

    fastio;

    int test_cases;
    string key;

    cin >> test_cases;
    cin >> key;

    map<char, char> mp;
    // for (int i = 0; i < count; i++)
    // {
    //     /* code */
    // }

    string input;

    while (test_cases--)
    {
        cin >> input;
        cout <<
    }
}
